# Get your key at https://hevy.com/settings?developer (requires Hevy PRO).
HEVY_API_KEY=sk_live_replace_me

# Optional: override the API base for testing / mocking.
# HEVY_API_BASE=https://api.hevyapp.com/v1

# Optional: HTTP transport bind for remote / claude.ai connector mode.
# HEVY_MCP_HOST=0.0.0.0
# HEVY_MCP_PORT=8000
